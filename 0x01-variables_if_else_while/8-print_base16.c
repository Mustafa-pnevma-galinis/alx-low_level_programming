#include <stdlib.h>
#include <time.h>
#include <stdio.h>
#include<string.h>
/* more headers goes there */

/* betty style doc for function main goes there */
int main(void)
{
	 int cnt = 0;

	while (cnt < 48){
		if (i<10){
			putchar(i + '0');
		}
		else if (i > 41){
			putchar(i -10 + 'A');
		}
		i++;
	}
	return (0);
}
