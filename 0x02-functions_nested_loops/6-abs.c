#include "main.h"

/**
* _abs - return the absolute value of number.
* @num: is a number.
* Return: The abs.
*/

int _abs(int number){
  if (number < 0){
    number = number * -1;
    return (number);
  }
  else{
    return (number);
  }
 
}
