#include "main.h"
/**
* print_numbers - return 1 if the provided character is uppercase and 0 if else.
*
* Return: void.
*/

void print_numbers(void)
{
    for (int i = 48; i < 58; i++)
    {
            _putchar(i);
    }
        _putchar('\n');
}
