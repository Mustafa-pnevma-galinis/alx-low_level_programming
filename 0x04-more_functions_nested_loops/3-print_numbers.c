#include "main.h"
/**
* print_numbers - return 1 if the provided character is uppercase and 0 if else.
*
* Return: void.
*/

void print_numbers(void)
{
    char c;
    for (int i = 48; i < 58; i++)
    {
            c = i
            _putchar(c);
    }
        _putchar('\n');
}
