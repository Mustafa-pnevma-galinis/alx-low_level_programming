#include <main.h>

/**
* print_line - prints numbers 10 times.
* @n: number of times
* Return: void.
*/

void print_line(int n)
{
    if (n > 0)
    {
        for (int i = 0; i < n; i++)
    {
        _putchar('_');
    }
      
    }
         _putchar('\n'); 
}
